export enum SubscriptionType {
  Classic = 'ribpay_classic',
  Plus = 'ribpay_plus',
}

export enum UserRole {
  Admin = 'admin',
  User = 'user',
}

export enum AccountType {
  personnePhysique,
  personneMorale,
}
